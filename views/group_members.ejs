<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Group Members</title>
    <%- include("./partials/head") %>
  </head>
  <body>
    <%- include("./partials/navBar") %>
    <div class="container my-4">
      <h1 class="display-4 fw-bold text-center text-primary">Group Members</h1>
      <p class="lead text-muted text-center mt-2 mb-4" style="font-size: 1.75rem">
        Get to Know the Minds Driving Our Research Forward
      </p>
      <% if (locals.groupMembers) { %>
      <div class="row row-cols-2 g-4 mb-4">
        <% groupMembers.forEach(groupMember => { %>
        <div class="col">
          <div
            class="card border-0 shadow-lg p-4 rounded h-100 d-flex flex-column"
            style="background-color: #f9f9f9"
          >
            <div class="d-flex align-items-center">
              <!-- Profile Image -->
              <div
                class="rounded-circle overflow-hidden me-3 border flex-shrink-0"
                style="width: 100px; height: 100px; border: 3px solid #007bff"
              >
                <img
                  src="https://cdn.jsdelivr.net/gh/AaradhyaNagar/Hamelberg_Public_Assets<%= groupMember.profileImageURL %>"
                  class="img-fluid"
                  alt="Profile Image"
                  style="object-fit: cover; width: 100%; height: 100%"
                />
              </div>
              <!-- Name, Title, and Education -->
              <div>
                <h5 class="fw-bold mb-1 text-primary">
                  <%= groupMember.name %>
                </h5>
                <p class="mb-1 text-muted"><%= groupMember.title %></p>

                <% if (groupMember.education_1) { %>
                <small class="text-secondary d-block"
                  ><%= groupMember.education_1 %></small
                >
                <% } %>

                <% if (groupMember.education_2) { %>
                <small class="text-secondary d-block"
                  ><%= groupMember.education_2 %></small
                >
                <% } %>

                <% if (groupMember.education_3) { %>
                <small class="text-secondary d-block"
                  ><%= groupMember.education_3 %></small
                >
                <% } %>
              </div>
            </div>
          </div>
        </div>
        <% }) %>
      </div>
      <% } %>
    </div>
    <%- include("./partials/footer") %>
    <%- include("./partials/script") %>
  </body>
</html>